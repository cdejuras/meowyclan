Feature: Backend tests and CI

- Added `ProductServiceTest` unit tests using Mockito and JUnit 5.
- Added GitHub Actions workflow `.github/workflows/ci.yml` to run backend tests against a Postgres service.

Rationale: Ensure test coverage and automated test runs on PRs.
